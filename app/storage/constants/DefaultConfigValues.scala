package storage.constants

object DefaultConfigValues {
  val defaultHost = "localhost"
  val defaultPort = 27017
  val defaultUser = "admin"
  val defaultPassword = ""
  val defaultDbName = "chat"
  val defaultMessagesCollection = "messages"
  val defaultTopicsCollection = "topics"
  val defaultUsersCollection = "users"
}
